<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yinggu.plane.mapper.FlightMapper">
    <resultMap id="BaseResultMap" type="com.yinggu.plane.pojo.Flight">
        <id column="id" jdbcType="INTEGER" property="id" />
        <result column="flightNum" jdbcType="VARCHAR" property="flightNum" />
        <result column="origin" jdbcType="VARCHAR" property="origin" />
        <result column="destination" jdbcType="VARCHAR" property="destination" />
        <result column="startTime" jdbcType="TIMESTAMP" property="startTime" />
        <result column="arrivalTime" jdbcType="TIMESTAMP" property="arrivalTime" />
        <result column="amount" jdbcType="INTEGER" property="amount" />
        <result column="price" jdbcType="INTEGER" property="price" />
    </resultMap>

<!--    <select id="selectList" resultMap="BaseResultMap">-->
<!--        select-->
<!--        *-->
<!--        from flight-->
<!--    </select>-->

    <select id="selectById" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        select
        id,
        flightNum,
        origin,
        destination,
--         DATE_FORMAT(startTime,'%Y-%m-%d'),
--         DATE_FORMAT(arrivalTime,'%Y-%m-%d'),
        startTime,
        arrivalTime,
        amount,
        price
        from flight
        where id=#{id,jdbcType=INTEGER}
    </select>

    <select id="selectList" parameterType="com.yinggu.plane.pojo.Flight" resultMap="BaseResultMap">
        select id,
               flightNum,
               origin,
               destination,
--                DATE_FORMAT(startTime,'%Y-%m-%d'),
--                DATE_FORMAT(arrivalTime,'%Y-%m-%d'),
                startTime,
                arrivalTime,
               amount,
               price
        from flight
            where 1=1
        <if test="origin != null" >
            and origin=#{origin,jdbcType=VARCHAR}
        </if>
        <if test="destination != null" >
            and destination=#{destination,jdbcType=VARCHAR}
        </if>
        <if test="startTime != null" >
            and DATE_FORMAT(startTime,'%Y-%m-%d')=DATE_FORMAT(#{startTime,jdbcType=VARCHAR},'%Y-%m-%d')
        </if>
    </select>

    <update id="updateById" parameterType="com.yinggu.plane.pojo.Flight">
        update flight
        set flightNum = #{flightNum,jdbcType=VARCHAR},
            origin = #{origin,jdbcType=VARCHAR},
            destination = #{destination,jdbcType=VARCHAR},
            startTime = #{startTime,jdbcType=TIMESTAMP},
            arrivalTime = #{arrivalTime,jdbcType=TIMESTAMP},
            amount = #{amount,jdbcType=INTEGER},
            price = #{price,jdbcType=INTEGER}
        where id = #{id,jdbcType=INTEGER}
    </update>



</mapper>